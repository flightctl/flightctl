apiVersion: flightctl.io/v1beta1
kind: Fleet
metadata:
  name: resourcesync-test-fleet-1-{{.TestID}}
spec:
  selector:
    matchLabels:
      fleet: resourcesync-test-1-{{.TestID}}
  template:
    spec:
      os:
        image: quay.io/fedora/fedora-bootc:42
