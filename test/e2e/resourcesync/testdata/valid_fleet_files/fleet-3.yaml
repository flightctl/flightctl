apiVersion: flightctl.io/v1beta1
kind: Fleet
metadata:
  name: resourcesync-test-fleet-3-{{.TestID}}
spec:
  selector:
    matchLabels:
      fleet: resourcesync-test-3-{{.TestID}}
  template:
    spec:
      os:
        image: quay.io/fedora/fedora-bootc:42
