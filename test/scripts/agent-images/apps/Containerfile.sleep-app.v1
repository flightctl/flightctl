# empty base image
FROM scratch

COPY --from=common test-podman-compose-sleep-work.yaml /podman-compose.yaml

# set the working directory to /
WORKDIR /

# labes generated by the build process consumed from metadata.json
LABEL app="sleep" \
      appType="compose" \
      version="0.0.1" \
      description="Sleep demo" \
      maintainer-name="John Doe" \
      maintainer-email="john@doe.com" \
