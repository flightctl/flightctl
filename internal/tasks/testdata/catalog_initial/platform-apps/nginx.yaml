apiVersion: flightctl.io/v1alpha1
kind: CatalogItem
metadata:
  name: nginx
  catalog: platform-apps
spec:
  type: quadlet
  reference:
    uri: quay.io/nginx/nginx
  versions:
    - version: "1.27.0"
      tag: v1.27.0
      channels:
        - stable
