apiVersion: flightctl.io/v1alpha1
kind: CatalogItem
metadata:
  name: prometheus
  catalog: platform-apps
spec:
  type: quadlet
  artifacts:
    - type: container
      uri: quay.io/prometheus/node-exporter
  versions:
    - version: "1.8.2"
      references:
        container: "v1.8.2"
      channels:
        - stable
