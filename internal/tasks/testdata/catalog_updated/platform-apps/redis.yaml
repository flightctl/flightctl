apiVersion: flightctl.io/v1alpha1
kind: CatalogItem
metadata:
  name: redis
  catalog: platform-apps
spec:
  type: quadlet
  artifacts:
    - type: container
      uri: quay.io/redis/redis
  versions:
    - version: "7.4.0"
      references:
        container: "v7.4.0"
      channels:
        - stable
