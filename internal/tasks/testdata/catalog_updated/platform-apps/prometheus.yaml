apiVersion: flightctl.io/v1alpha1
kind: CatalogItem
metadata:
  name: prometheus
  catalog: platform-apps
spec:
  type: quadlet
  artifacts:
    - type: container
      uri: quay.io/prometheus/node-exporter
  versions:
    - version: "1.9.0"
      references:
        container: "v1.9.0"
      channels:
        - stable
