apiVersion: flightctl.io/v1beta1
kind: Repository
metadata:
  name: flightctl-http-git
spec:
  url: https://github.com/flightctl/flightctl.git
  type: http
  httpConfig:
    username: myusername
    password: mypassword
    skipServerVerification: true