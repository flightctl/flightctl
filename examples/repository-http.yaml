apiVersion: flightctl.io/v1beta1
kind: Repository
metadata:
  name: flightctl-http
spec:
  url: https://my-server.com/flightctl
  type: http
  httpConfig:
    username: myusername
    password: mypassword
    skipServerVerification: true