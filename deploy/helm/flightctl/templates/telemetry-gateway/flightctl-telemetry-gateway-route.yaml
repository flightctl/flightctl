{{ if eq (include "flightctl.getServiceExposeMethod" .) "route" }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    {{- with .Values.global.additionalRouteLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
    {{- with .Values.telemetryGateway.additionalRouteLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
    {{- include "flightctl.standardLabels" . | nindent 4 }}
    flightctl.service: flightctl-telemetry-gateway
  name: flightctl-telemetry
  namespace: {{ .Release.Namespace }}
spec:
  host: telemetry.{{ include "flightctl.getBaseDomain" . }}
  port:
    targetPort: 4317
  tls:
    termination: passthrough
    insecureEdgeTerminationPolicy: None
  to:
    kind: Service
    name: flightctl-telemetry-gateway
    weight: 100
  wildcardPolicy: None
{{- end }}
