apiVersion: v1
kind: Service
metadata:
  labels:
    flightctl.service: flightctl-api
    {{- include "flightctl.standardLabels" . | nindent 4 }}
  name: flightctl-api-agent
  namespace: {{ .Release.Namespace }}
spec:
  {{- if and (eq (include "flightctl.getServiceExposeMethod" .) "nodePort") .Values.dev.nodePorts.agent }}
  type: NodePort
  {{- end }}
  ports:
    - name: "agent-api"
      port: 7443
      targetPort: 7443
      {{- if and (eq (include "flightctl.getServiceExposeMethod" .) "nodePort") .Values.dev.nodePorts.agent }}
      nodePort: {{ .Values.dev.nodePorts.agent }}
      {{- end }}
  selector:
    flightctl.service: flightctl-api
