{{ if eq (include "flightctl.getServiceExposeMethod" .) "gateway" }}
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: flightctl-gateway
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "flightctl.standardLabels" . | nindent 4 }}
spec:
  gatewayClassName: {{ .Values.global.gateway.gatewayClassName }}
  listeners:
  {{- if .Values.ui.enabled }}
  - name: ui
    hostname: ui.{{ include "flightctl.getBaseDomain" . }}
    {{- if .Values.global.baseDomainTlsSecretName }}
    port: {{ .Values.global.gateway.ports.tls }}
    protocol: HTTPS
    tls:
      mode: Terminate
      certificateRefs:
      - kind: Secret
        name: {{ .Values.global.baseDomainTlsSecretName }}
    {{- else }}
    port: {{ .Values.global.gateway.ports.http }}
    protocol: HTTP
    {{- end }}
  - name: cli-artifacts
    hostname: cli-artifacts.{{ include "flightctl.getBaseDomain" . }}
    {{- if .Values.global.baseDomainTlsSecretName }}
    port: {{ .Values.global.gateway.ports.tls }}
    protocol: HTTPS
    tls:
      mode: Terminate
      certificateRefs:
      - kind: Secret
        name: {{ .Values.global.baseDomainTlsSecretName }}
    {{- else }}
    port: {{ .Values.global.gateway.ports.http }}
    protocol: HTTP
    {{- end }}
  {{- end }}
  - name: api
    hostname: '*.{{ include "flightctl.getBaseDomain" . }}'
    port: {{ .Values.global.gateway.ports.tls }}
    protocol: TLS
    tls:
      mode: Passthrough
{{ end }}