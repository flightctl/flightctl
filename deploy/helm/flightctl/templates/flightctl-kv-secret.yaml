kind: Secret
apiVersion: v1
metadata:
  name: {{ $.Release.Name }}-kv-secret
  namespace: {{ .Release.Namespace }}
type: kubernetes.io/basic-auth
stringData:
  password: {{ .Values.redis.password | quote }}