# Helm chart overrides for community-el9 flavor
# This file contains community-el9 specific chart metadata and configurations

# Chart metadata (if needed)
# annotations:
#   community.edition: "true"
#   target.platform: "el9"

# Image overrides for this flavor would be handled via values.yaml merging
# For now, this file serves as a placeholder for any chart-level configuration
# that might be flavor-specific