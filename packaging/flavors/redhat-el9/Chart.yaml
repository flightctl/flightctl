# Helm chart value overrides for redhat-el9 flavor
# This file is REQUIRED - Red Hat flavors must have Chart.yaml overrides
# If missing, the render pipeline will fail with an error

annotations:
  redhat.edition: "true"
  target.platform: "rhel9"
  security.compliance: "fips-140-2"