# Helm chart overrides for redhat-el10 flavor
# This file is optional - if present, it replaces deploy/helm/flightctl/Chart.yaml
# If absent, the original Chart.yaml remains authoritative

annotations:
  redhat.edition: "true"
  target.platform: "rhel10"
  security.compliance: "fips-140-3"