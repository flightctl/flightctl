# Helm chart value overrides for redhat-el10 flavor
# This file is optional - if present, values are merged into generated Chart.yaml
# If absent, the generated Chart.yaml remains unmodified

annotations:
  redhat.edition: "true"
  target.platform: "rhel10"
  security.compliance: "fips-140-3"