# This file is intentionally left empty.
# It is included into flightctl.spec via %include %{S:1} and can be used to
# declare additional sources, patches, and macro overrides without modifying
# the spec itself.
